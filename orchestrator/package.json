{
  "name": "@bount-mcp/orchestrator",
  "version": "0.1.0",
  "description": "MCP-of-MCPs: composes aep-core, site-crawler, and industry-news-tracker into higher-level workflows. Holds no secrets itself.",
  "private": true,
  "main": "dist/router.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/router.js",
    "dev": "ts-node src/router.ts",
    "test": "node --test --loader ts-node/esm tests/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2",
    "@types/node": "^20.14.0"
  }
}
